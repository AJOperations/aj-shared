# aj-shared contributor instructions

Read `README.md` and `pyproject.toml` before changing this shared package. This
is Platform-level work: preserve public contract compatibility and test Flask
and FastAPI paths with safe local data. Do not add credentials, publish a
package, change repository visibility, or alter downstream applications without
separate approval.
